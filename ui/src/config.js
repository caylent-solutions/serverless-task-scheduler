/**
 * Application configuration
 */

// API base URL - all backend API calls should use this prefix
// Use absolute path to ensure it works regardless of current page location
export const API_BASE_URL = '/dev/api';

export default {
  API_BASE_URL
};
